import { FilterConfigPollPipe } from './filter-config-poll.pipe';

describe('FilterConfigPollPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConfigPollPipe();
    expect(pipe).toBeTruthy();
  });
});
